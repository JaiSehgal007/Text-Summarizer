transformers
transformers[sentencepiece] 
datasets
sacrebleu 
rouge_score
py7zr
pandas
nltk
tqdm
PyYAML
matplotlib
torch
notebook
boto3
mypy-boto3-s3
python-box==6.0.2
ensure==1.0.2
fastapi==0.78.0
uvicorn==0.18.3
Jinja2==3.1.2
-e . 

# sacrebleu -> #this is one of the evalaation matrix for the for text summarization, for it we use a matrix called row matrix, and to use row matrix we need this library
# this will install the setup.py, where we will be writing the login for local 
# package installation code, and install by command pip install -r requirements.txt